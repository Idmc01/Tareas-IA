name: cnn_scratch
type: classifier

architecture:
  base: resnet18_partial
  layers: [conv1, conv2_x, conv3_x]
  features_dim: 128

classifier:
  hidden_dim: 64
  dropout: 0.3

training:
  optimizer: adamw
  lr: ${train.lr}
  weight_decay: 1e-4
  scheduler: cosine
